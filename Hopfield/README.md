# MAI lab - Hopfield network

## Requirements

    python3 -m pip install -r requirements.txt

## Using

### train:
- `python3 lab.py`;
- it creates image files.

### predict:
- manually corrupt image file (like in examples);
- `python3 lab.py <corrupted file>`;
- it corrects image and recognizes letter.

